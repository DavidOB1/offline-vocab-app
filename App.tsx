import React from "react";
import MainContainer from "./src/navigation/MainContainer";

// Directs the main app to the MainContainer navigator
const App = () => <MainContainer />;

export default App;
